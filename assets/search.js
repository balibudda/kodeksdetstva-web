// Клиентский поиск по ситуации. Индекс — /search-index.json (создаётся при сборке).
// Устойчив к падежам и формам слова (лёгкий стеммер) и к разговорным
// формулировкам (словарь синонимов). Работает офлайн.
(function () {
  var input = document.getElementById('q')
  var out = document.getElementById('results')
  if (!input || !out) return

  var INDEX = []
  var ready = false

  // разговорное / формы → к стеммам из контента
  var SYN = {
    'похит': 'похищени пропа розыск увезл забрал',
    'похищ': 'похищени пропа розыск',
    'украл': 'похищени пропа розыск',
    'выкрал': 'похищени пропа розыск',
    'забрал': 'похищени родительск увезл',
    'увезл': 'похищени родительск выезд',
    'пропал': 'пропа потерял розыск похищени',
    'потерял': 'пропа потерял розыск',
    'сбежал': 'ушел из дома побег',
    'убежал': 'ушел из дома побег',
    'бьет': 'насил наказан побои шлеп ремен',
    'бьют': 'насил наказан побои',
    'избил': 'насил побои истязан',
    'ударил': 'насил наказан побои',
    'орет': 'крик эмоциональн насил унижен',
    'орут': 'крик эмоциональн насил',
    'кричит': 'крик эмоциональн насил',
    'ругает': 'крик оскорблен унижен',
    'суицид': 'суицидальн покончить не хочет жить самоубий',
    'самоубийство': 'суицидальн покончить самоубий',
    'вскрыл': 'самоповрежд селфхарм порез',
    'режет': 'самоповрежд селфхарм порез',
    'порезы': 'самоповрежд селфхарм',
    'булли': 'травл буллинг',
    'буллинг': 'травл',
    'травят': 'травл буллинг',
    'дразнят': 'травл буллинг',
    'нюдсы': 'интимн фото шантаж sextortion',
    'фотки': 'интимн фото шантаж',
    'шантаж': 'шантаж вымогат sextortion',
    'закладки': 'вещест наркотик сбыт вербовк',
    'закладчик': 'вещест наркотик сбыт вербовк',
    'наркотики': 'вещест наркотик закладк зависим',
    'вейп': 'вейп никотин',
    'снюс': 'никотин вещест',
    'курит': 'вейп никотин сигарет',
    'пьет': 'алкогол компан отравлен',
    'бухает': 'алкогол компан',
    'казино': 'ставк азартн игров зависим лудоман',
    'ставки': 'ставк азартн игров зависим',
    'играет': 'игров зависим ставк казино',
    'прививки': 'прививок вакцинац отказ иммунизац информированн соглас',
    'прививка': 'прививок вакцинац отказ',
    'вакцина': 'прививок вакцинац отказ',
    'манту': 'прививок иммунодиагностик фтизиатр',
    'слежка': 'контрол геолокац прослушк приватн слеж',
    'следят': 'контрол геолокац приватн слеж',
    'развод': 'развод родители порознь место жительств',
    'алименты': 'алимент содержан',
    'полиция': 'допрос задержан несовершеннолетн полиц',
    'допрос': 'допрос несовершеннолетн полиц',
    'задержали': 'задержан доставлен досмотр полиц',
    'кдн': 'кдн пдн учет',
    'депрессия': 'депресс апат ничего не хочет',
    'аборт': 'беременност аборт прерыван',
    'залет': 'беременност аборт',
    'беременна': 'беременност аборт',
    'изнасилов': 'насил сексуализирован растлен',
    'растлен': 'насил сексуализирован растлен груминг',
    'педофил': 'насил сексуализирован груминг растлен',
    'секта': 'секта наставник вовлечен религи',
    'религия': 'религи принужд свобода совести',
    'первая любовь': 'первая любовь отвержен расставан',
    'расстались': 'отвержен расставан первая любовь',
    'деньги': 'карманн деньги финанс',
    'ночью': 'поздно домой комендантск тревог',
    'гулять': 'поздно домой комендантск',
    'драк': 'драк травм побои перелом первая помощь',
    'дерут': 'драк травм побои',
    'побил': 'драк побои травм насил',
    'ударил': 'драк побои травм насил наказан',
    'избил': 'побои травм насил истязан',
    'перелом': 'перелом травм травмпункт первая помощь',
    'сломал': 'перелом травм травмпункт',
    'упал': 'травм ушиб перелом первая помощь',
    'поранил': 'рана травм порез первая помощь',
    'рассек': 'рана травм швы травмпункт',
    'разбил голов': 'травм голов сотрясен',
    'сотрясен': 'травм голов сотрясен',
    'травм': 'травм перелом драк первая помощь травмпункт',
    'кровь': 'кровотечен рана первая помощь',
    'мигрант': 'мигрант',
    'приемный': 'приемн усыновл',
    'граница': 'выезд граница согласие',
    'заграницу': 'выезд граница согласие',
    'за границу': 'выезд граница согласие',
  }

  var ENDINGS = [
    'ами', 'ями', 'ого', 'его', 'ому', 'ему', 'ыми', 'ими', 'ах', 'ях',
    'ов', 'ев', 'ий', 'ый', 'ой', 'ая', 'яя', 'ое', 'ее', 'ые', 'ие',
    'ам', 'ям', 'ом', 'ем', 'ым', 'им', 'их', 'ых', 'ешь', 'ет', 'ут',
    'ют', 'ат', 'ят', 'ла', 'ло', 'ли', 'на', 'но', 'ны',
    'а', 'я', 'о', 'е', 'у', 'ю', 'ы', 'и', 'й', 'ь',
  ]

  function norm(s) {
    return (s || '')
      .toLowerCase()
      .replace(/ё/g, 'е')
      .replace(/[^a-zа-я0-9 ]/gi, ' ')
      .replace(/\s+/g, ' ')
      .trim()
  }

  function stem(w) {
    if (w.length <= 4) return w
    for (var i = 0; i < ENDINGS.length; i++) {
      var e = ENDINGS[i]
      if (w.length - e.length >= 4 && w.slice(-e.length) === e) return w.slice(0, -e.length)
    }
    return w
  }

  function tokens(s) {
    return norm(s).split(' ').filter(Boolean).map(stem)
  }

  function expand(q) {
    var base = norm(q)
    var out = tokens(q)
    for (var k in SYN) {
      if (base.indexOf(k) !== -1) out = out.concat(tokens(SYN[k]))
    }
    // уникальные, длиной >= 3
    var seen = {}
    return out.filter(function (t) {
      if (t.length < 3 || seen[t]) return false
      seen[t] = 1
      return true
    })
  }

  function fieldHit(fieldTokens, fieldStr, term) {
    for (var i = 0; i < fieldTokens.length; i++) {
      if (fieldTokens[i] === term) return 2 // точное совпадение стемма
      if (fieldTokens[i].indexOf(term) === 0 || term.indexOf(fieldTokens[i]) === 0) return 1
    }
    if (term.length >= 4 && fieldStr.indexOf(term) !== -1) return 1
    return 0
  }

  function score(item, terms) {
    var sc = 0
    for (var i = 0; i < terms.length; i++) {
      var term = terms[i]
      sc += 10 * fieldHit(item._t, item._ts, term)
      sc += 6 * fieldHit(item._k, item._ks, term)
      sc += 4 * fieldHit(item._d, item._ds, term)
      sc += 1 * fieldHit(item._x, item._xs, term)
      sc += 2 * fieldHit(item._s, item._ss, term)
    }
    if (sc > 0 && item.urgent) sc += 3
    return sc
  }

  function prep(data) {
    return data.map(function (it) {
      it._t = tokens(it.t); it._ts = norm(it.t)
      it._k = tokens(it.k); it._ks = norm(it.k)
      it._d = tokens(it.d); it._ds = norm(it.d)
      it._s = tokens(it.s); it._ss = norm(it.s)
      it._x = tokens(it.x); it._xs = norm(it.x)
      return it
    })
  }

  function escapeHtml(s) {
    return (s || '').replace(/[&<>"]/g, function (c) {
      return { '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;' }[c]
    })
  }

  function run() {
    var q = input.value.trim()
    if (!ready) {
      out.innerHTML = '<li class="search-empty">Загрузка…</li>'
      return
    }
    if (q.length < 2) {
      out.innerHTML = ''
      return
    }
    var terms = expand(q)
    var ranked = INDEX.map(function (it) { return { it: it, sc: score(it, terms) } })
      .filter(function (r) { return r.sc > 0 })
      .sort(function (a, b) { return b.sc - a.sc })
      .slice(0, 20)

    if (!ranked.length) {
      var subj = encodeURIComponent('Вопрос с сайта: ' + q)
      var body = encodeURIComponent(
        'Здравствуйте! Не нашёл(-ла) на сайте ответ по теме: «' + q + '».\n\nВопрос: \n\n(Если хотите получить ответ, оставьте, пожалуйста, тут же обратный адрес или контакт для связи.)',
      )
      out.innerHTML =
        '<li class="search-empty">Ничего не нашлось. Попробуйте другими словами (например «похищение», «травля», «не хочет жить») или откройте <a href="/">разделы</a>. Если это срочно — <a href="/pomoshch/">Помощь сейчас</a>.' +
        '<div class="ask-question"><p><b>Не нашли ответ на свой вопрос?</b> Задайте его — мы постараемся сформулировать ответ и добавить тему на сайт.</p>' +
        '<a class="btn btn-ghost" href="mailto:info@kodeksdetstva.ru?subject=' + subj + '&body=' + body + '">✉️ Задать вопрос</a></div></li>'
      return
    }
    out.innerHTML = ranked
      .map(function (r) {
        var it = r.it
        return (
          '<li><a href="' + it.u + '">' +
          '<span class="r-title">' + escapeHtml(it.t) + (it.urgent ? ' — срочное' : '') + '</span>' +
          '<span class="r-sec">' + escapeHtml(it.s) + '</span>' +
          '<span class="r-desc">' + escapeHtml(it.d) + '</span>' +
          '</a></li>'
        )
      })
      .join('')
  }

  fetch('/search-index.json')
    .then(function (r) { return r.json() })
    .then(function (data) {
      INDEX = prep(data)
      ready = true
      run()
    })
    .catch(function () {
      out.innerHTML = '<li class="search-empty">Не удалось загрузить поиск. Откройте <a href="/">разделы</a>.</li>'
    })

  var t
  input.addEventListener('input', function () {
    clearTimeout(t)
    t = setTimeout(run, 110)
  })

  var pre = new URLSearchParams(location.search).get('q')
  if (pre) {
    input.value = pre
    run()
  }
})()
